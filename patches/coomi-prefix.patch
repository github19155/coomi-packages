diff --git a/scripts/properties.sh b/scripts/properties.sh
--- a/scripts/properties.sh
+++ b/scripts/properties.sh
@@ -464,6 +464,6 @@
 # Default value: `com.termux`
 ##
-TERMUX_APP__PACKAGE_NAME="com.termux"
+TERMUX_APP__PACKAGE_NAME="com.coomi.android"
 TERMUX_APP_PACKAGE="$TERMUX_APP__PACKAGE_NAME" # Deprecated alternative variable for `TERMUX_APP__PACKAGE_NAME`
 
 __termux_build_props__add_variables_validator_actions "TERMUX_APP__PACKAGE_NAME" "app_package_name"
@@ -2181,10 +2181,10 @@
 # FIXME: Checking for all variables will be added later in repo
 # changes pull, currently only `TERMUX_REPO_APP__PACKAGE_NAME` is checked.
-TERMUX_REPO_APP__PACKAGE_NAME="com.termux"
-TERMUX_REPO_APP__DATA_DIR="/data/data/com.termux"
-TERMUX_REPO__CORE_DIR="/data/data/com.termux/termux/core"
-TERMUX_REPO__APPS_DIR="/data/data/com.termux/termux/app"
-TERMUX_REPO__ROOTFS="/data/data/com.termux/files"
-TERMUX_REPO__HOME="/data/data/com.termux/files/home"
-TERMUX_REPO__PREFIX="/data/data/com.termux/files/usr"
+TERMUX_REPO_APP__PACKAGE_NAME="com.coomi.android"
+TERMUX_REPO_APP__DATA_DIR="/data/data/com.coomi.android"
+TERMUX_REPO__CORE_DIR="/data/data/com.coomi.android/termux/core"
+TERMUX_REPO__APPS_DIR="/data/data/com.coomi.android/termux/app"
+TERMUX_REPO__ROOTFS="/data/data/com.coomi.android/files"
+TERMUX_REPO__HOME="/data/data/com.coomi.android/files/home"
+TERMUX_REPO__PREFIX="/data/data/com.coomi.android/files/usr"
 
@@ -2234,6 +2234,6 @@
 # Path to CGCT tools
-CGCT_DEFAULT_PREFIX="/data/data/com.termux/files/usr/glibc"
+CGCT_DEFAULT_PREFIX="/data/data/com.coomi.android/files/usr/glibc"
 __termux_build_props__add_variables_validator_actions "CGCT_DEFAULT_PREFIX" "safe_absolute_path invalid_termux_prefix_paths"
 
-export CGCT_DIR="/data/data/com.termux/cgct"
+export CGCT_DIR="/data/data/com.coomi.android/cgct"
 __termux_build_props__add_variables_validator_actions "CGCT_DIR" "safe_absolute_path invalid_termux_prefix_paths"
diff --git a/scripts/build/termux_step_start_build.sh b/scripts/build/termux_step_start_build.sh
--- a/scripts/build/termux_step_start_build.sh
+++ b/scripts/build/termux_step_start_build.sh
@@ -29,6 +29,7 @@
 	TERMUX_PKG_FULLVERSION=$TERMUX_PKG_VERSION
 	if [ "$TERMUX_PKG_REVISION" != "0" ] || [ "$TERMUX_PKG_FULLVERSION" != "${TERMUX_PKG_FULLVERSION/-/}" ]; then
 		# "0" is the default revision, so only include it if the upstream versions contains "-" itself
 		TERMUX_PKG_FULLVERSION+="-$TERMUX_PKG_REVISION"
 	fi
+	TERMUX_PKG_FULLVERSION+="+coomi1"
 	# full format version for pacman
diff --git a/packages/termux-exec/build.sh b/packages/termux-exec/build.sh
--- a/packages/termux-exec/build.sh
+++ b/packages/termux-exec/build.sh
@@ -40,3 +40,7 @@
-termux_step_post_massage() {
-	# Hack to compile `libtermux-exec_nos_c_tre_runtime-binary-tests` for api level 28 if default (currently 24) is less than it.
-	if [[ "$TERMUX_PKG_API_LEVEL" -lt 28 ]]; then
+termux_step_post_massage() {
+	local coomi_termux_exec_lib="$TERMUX_PKG_MASSAGEDIR/$TERMUX_PREFIX_CLASSICAL/bin/termux-exec-ld-preload-lib"
+	if [[ -f "$coomi_termux_exec_lib" ]]; then
+		sed -i "s#/data/data/com.termux#${TERMUX_APP__DATA_DIR}#g" "$coomi_termux_exec_lib"
+	fi
+	# Hack to compile `libtermux-exec_nos_c_tre_runtime-binary-tests` for api level 28 if default (currently 24) is less than it.
+	if [[ "$TERMUX_PKG_API_LEVEL" -lt 28 ]]; then
diff --git a/packages/termux-tools/build.sh b/packages/termux-tools/build.sh
--- a/packages/termux-tools/build.sh
+++ b/packages/termux-tools/build.sh
@@ -28,9 +28,25 @@
-termux_step_post_make_install() {
-	TERMUX_PKG_CONFFILES="$(cat "$TERMUX_PKG_BUILDDIR/conffiles")"
-}
-
-termux_step_create_debscripts() {
-	cat <<- EOF > ./preinst
-	$(cat "$TERMUX_PKG_BUILDDIR/preinst")
-	EOF
-}
+termux_step_post_make_install() {
+	TERMUX_PKG_CONFFILES="$(cat "$TERMUX_PKG_BUILDDIR/conffiles")"
+}
+
+coomi_termux_tools_rewrite_paths() {
+	local coomi_termux_tools_root="$TERMUX_PKG_MASSAGEDIR/$TERMUX_PREFIX_CLASSICAL"
+	while IFS= read -r -d '' coomi_termux_tools_file; do
+		if grep -Iq . "$coomi_termux_tools_file" && grep -Fq '/data/data/com.termux' "$coomi_termux_tools_file"; then
+			sed -i "s#/data/data/com.termux#${TERMUX_APP__DATA_DIR}#g" "$coomi_termux_tools_file"
+		fi
+	done < <(find "$coomi_termux_tools_root" -type f -print0)
+}
+
+termux_step_post_massage() {
+	coomi_termux_tools_rewrite_paths
+}
+
+termux_step_create_debscripts() {
+	cat <<- EOF > ./preinst
+	$(cat "$TERMUX_PKG_BUILDDIR/preinst")
+	EOF
+}
+	if grep -Fq '/data/data/com.termux' ./preinst; then
+		sed -i "s#/data/data/com.termux#${TERMUX_APP__DATA_DIR}#g" ./preinst
+	fi
